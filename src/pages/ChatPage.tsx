/** ChatPage — Stub (Phase 2); full UI built in Phase 3 */
import './pages.css';

export default function ChatPage() {
  return (
    <div className="stub-page" aria-label="Chat page placeholder">
      <span className="stub-page__icon" aria-hidden="true">💬</span>
      <h2 className="stub-page__title">Chat</h2>
      <p className="stub-page__sub">Conversation UI coming in Phase 3</p>
    </div>
  );
}
